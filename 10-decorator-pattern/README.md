# 10 - Decorator Pattern

![php-to-rust logo](../images/php-to-rust.png)

This example demonstrates the Decorator pattern in Rust, allowing dynamic composition of message formatting (brackets, exclamation marks) using trait objects.

## Run

From this folder:

```bash
cargo run
```

Expected output shows decorated message: `[Patterns!]`.
