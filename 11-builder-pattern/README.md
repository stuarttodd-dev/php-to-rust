# 11 - Builder Pattern

![php-to-rust logo](../images/php-to-rust.png)

This example implements the Builder pattern for constructing `WindowConfig` structs with optional fields, demonstrating fluent API design.

## Run

From this folder:

```bash
cargo run
```

Expected output shows the built window configuration.
