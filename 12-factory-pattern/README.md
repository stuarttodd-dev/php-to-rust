# 12 - Factory Pattern

![php-to-rust logo](../images/php-to-rust.png)

This example shows the Factory pattern for creating different animal types (Dog, Cat) based on an enum, using trait objects for polymorphism.

## Run

From this folder:

```bash
cargo run
```

Expected output: `woof` and `meow`.
