# 15 - Observer Pattern

![php-to-rust logo](../images/php-to-rust.png)

This example shows the Observer pattern for notifying multiple observers (logger, notifier) when a subject's state changes.

## Run

From this folder:

```bash
cargo run
```

Expected output shows observer notifications for state changes.
