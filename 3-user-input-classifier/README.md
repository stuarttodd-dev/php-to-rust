# 3 - User Input Classifier

This example demonstrates how to classify optional integer input values in Rust using `Option<i32>`.

## Run

From this folder:

```bash
cargo run
```

Expected output shows classification of values as positive/negative/zero and even/odd, plus a summary count.
